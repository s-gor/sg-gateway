#!/usr/bin/env sh
set -eu

KEEP_DATA="${SG_GATEWAY_KEEP_DATA:-1}"

echo "SG-Gateway uninstall placeholder"
echo "KEEP_DATA=$KEEP_DATA"
echo "This script is intentionally non-destructive in MVP."
echo "Future versions will require explicit confirmation before removing services or data."